
  <%= form_for(@task) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <tr>
        <th class="done">+</th>
        
          <th colspan="4" class="projects">
            <div class="input-group">
                <%= f.text_field :name, :class => 'form-control', placeholder: 'Start typing here to create a task...' %>
                <%= f.hidden_field :project_id, value: @project_id %> <!-- Add Task to direct Project -->
              <span class="input-group-btn">
                <%= f.submit 'Add Task', :class => 'btn btn-success addTask' %>
              </span>
            </div>
        </th>
    </tr>
  <% end %>
